<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- name space는 BookRepo의 fully qualified name으로 설정한다. -->
<mapper namespace="com.ssafy.D203.model.dao.UserDao">
	<insert id="insert" parameterType="User">
	 
	INSERT INTO user (userName,ready,userOrder,ranking,score) 
	VALUES (#{userName},#{ready},#{userOrder},#{ranking},#{score})
	 
	<!-- 
	 INSERT INTO user2
	    	(strName,intNum,doubleNum,boolTF)
		VALUES
	   		(#{strName},#{intNum},#{doubleNum},#{boolTF})
 	 -->
	 	</insert>
</mapper>